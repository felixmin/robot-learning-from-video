# @package _global_

defaults:
  - _stage2_base
  - /cluster@cluster: mcml_x100
  - optional /user_config: local

experiment:
  name: stage2_cluster
  description: "Cluster Stage 2 latent-flow training on the default Octo24 mix"

logging:
  tags: ["stage2", "lerobot_v3", "octo24", "latent_flow", "cluster"]
