interface:
  display_name: "LRZ Docker Refresh"
  short_description: "Build, push, import, archive, and activate the unified LRZ Enroot image."
  default_prompt: "Use $lrz-docker-enroot-refresh to run the unified LRZ Docker build/push workflow, import the image as a dated Enroot .sqsh on DSS with DSS scratch/cache paths, archive the previously configured image into enroot/old/, and update config/user_config/local.yaml."
