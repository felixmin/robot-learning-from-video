interface:
  display_name: "Only Implementation Testing"
  short_description: "Implement an approved plan and validate it cleanly."
  default_prompt: "Use $only-implementation-testing to execute an approved dated plan from docs/plan with targeted testing and final review."

policy:
  allow_implicit_invocation: true
