# Run Note: 2026-03-15_16-54-31_stage3_smolvla_4gpu_bs16

## Meta

- Date: 2026-03-15
- Status: failed
- Mode: cluster
- Host: login-04 (inspected via `ssh ai`; active compute host: `lrz-dgx-a100-001`)
- Code Commit: 509ed113608417477fb94574b951f6cdf0fd56fa
- Worktree State: clean
- Logical Cluster Target: lrz_x100
- Stage: stage3
- Script: `scripts/submit_job.py`
- Base Experiment: `stage3_cluster`
- Config Path: `config/experiment/runs/2026-03-15_16-54-31_stage3_smolvla_4gpu_bs16.yaml`
- Experiment Name: `stage3_smolvla_4gpu_bs16`
- Intended Run Dir: `/dss/dssmcmlfs01/pn57pi/pn57pi-dss-0001/felix_minzenmay/runs/2026-03-15_16-54-31_stage3_smolvla_4gpu_bs16`
- Final Run Dir: `/dss/dssmcmlfs01/pn57pi/pn57pi-dss-0001/felix_minzenmay/runs/2026-03-15_16-54-31_stage3_smolvla_4gpu_bs16`
- LRZ Job ID: `5519210`
- MCML Job ID:

## Purpose

- Run Stage 3 LeRobot training on the full Libero multitask dataset with the HLRP shared SmolVLA policy in `action_scratch` mode.
- Use a 4x A100 LRZ allocation with global batch size `16` for the `felixmin/hlrp_smolvla_shared_libero_scratch` policy run.
- This job was already launched before the documented run config existed, so this note and config capture the live run post hoc and keep the resolved run directory fixed to the active cluster path.

## Config Delta Vs Default

- Override `stage3_profile` from `multitask_scratch` to `action_scratch`, which switches Stage 3 training from multitask to action-only scratch mode.
- Override the cluster from `mcml_x100` to `lrz_x100`.
- Increase `cluster.compute.gpus_per_node` from `1` to `4`.
- Change `cluster.compute.time_limit` from `15:00:00` to `06:20:00`.
- Override `lerobot.batch_size` to `16`; the selected `action_scratch` profile would otherwise resolve to `64`.
- Set `experiment.name` to `stage3_smolvla_4gpu_bs16`, matching the live submitted job.
- Pin `logging.runs_dir` to `/dss/dssmcmlfs01/pn57pi/pn57pi-dss-0001/felix_minzenmay/runs/2026-03-15_16-54-31_stage3_smolvla_4gpu_bs16` so the documented config reproduces the existing run directory exactly.
- Update `experiment.description` for this specific run.

## Upstream Artifacts / Checkpoints

- Type: `LAM checkpoint`
  Source Run: `none`
  Path: `null`
  Notes: `action_scratch` does not reuse a LAM checkpoint or Stage 2 artifact; the resolved config shows `lam_checkpoint_path: null`.

## Launch Command

```bash
python scripts/submit_job.py \
  experiment=stage3_cluster \
  stage3_profile=action_scratch \
  cluster=lrz_x100 \
  cluster.compute.time_limit=06:20:00 \
  cluster.compute.gpus_per_node=4 \
  lerobot.batch_size=16 \
  experiment.name=stage3_smolvla_4gpu_bs16
```

## Results / Findings

- Final Slurm status: `FAILED` with exit code `1:0` after `01:38:32` on `lrz-dgx-a100-001` (`5519210`, partition `lrz-dgx-a100-80x8`).
- The run trained cleanly through step `20000`; `5519210.err` shows loss improving from roughly `0.066` early to `0.021-0.022` by step `20K`, with `batch_sup action=1.000` throughout the logged training window.
- The step-20000 checkpoint completed successfully before failure. The run directory contains `lerobot/checkpoints/020000/{pretrained_model,training_state}`, and `training_state/training_step.json` records `{"step": 20000}`.
- Failure happened immediately after eval started: `Checkpoint policy after step 20000` at `2026-03-15 18:21:07`, `Eval policy at step 20000` at `18:21:12`, then NCCL watchdog timeouts at `18:31:12` after `600000 ms`.
- The concrete error is a distributed collective timeout: ranks 1-3 report `WorkNCCL(... OpType=ALLREDUCE ...) ran for 6000xx milliseconds before timing out`, followed by `ChildFailedError` from the `accelerate` launcher.
- Eval produced only partial artifacts before hanging. `lerobot/eval/videos_step_020000/libero_10_0/` contains four videos (`eval_episode_0.mp4` to `eval_episode_3.mp4`) written between `18:23:52` and `18:26:25`; no aggregated eval JSON or broader per-task output was written.
- Most likely cause: in the training loop only the main process enters `eval_policy_all(...)`, while the other ranks proceed to `accelerator.wait_for_everyone()`. With 4 GPUs, `libero_10`, `eval.n_episodes=10`, `eval.batch_size=2`, and `max_parallel_tasks=1`, rank 0 appears to spend longer than the 10-minute NCCL watchdog budget inside eval, so the non-main ranks time out waiting at the distributed barrier.
- The cluster launcher resolved to `accelerate-multi-gpu(4)` and invoked `/opt/hlrp-venv/bin/lerobot-train --config_path /dss/dssmcmlfs01/pn57pi/pn57pi-dss-0001/felix_minzenmay/runs/2026-03-15_16-54-31_stage3_smolvla_4gpu_bs16/lerobot_train_config.json`.
- Resolved policy repo id: `felixmin/hlrp_smolvla_shared_libero_scratch`.
- Resolved dataset repo id: `hlrp/libero_full_multitask`.
- Active container image: `/dss/dssmcmlfs01/pn57pi/pn57pi-dss-0001/felix_minzenmay/enroot/hlrp_unified_cu128_imported_2026-03-15_160458.sqsh`.
- W&B run path: `felixmin/lerobot/ah6a4927`.
- The W&B metadata snapshot provided at launch time lists `seed:1000`, while the stored Hydra config in `.hydra/config.yaml` currently resolves `seed: 42`; verify downstream reporting against the run directory if seed provenance matters later.
