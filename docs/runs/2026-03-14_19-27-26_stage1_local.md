# Run Note: 2026-03-14_19-27-26_stage1_local

## Meta

- Date: 2026-03-14
- Status: completed
- Mode: local
- Host: tueilsy-st-022
- Code Commit: 493ce374be3dc63af5c48c4cb2d2c43cb506975f
- Worktree State: unknown (W&B metadata recorded the commit but not the dirty/clean state)
- Logical Cluster Target:
- Stage: stage1
- Script: `scripts/2_train_stage1_lam.py`
- Base Experiment: `stage1_local`
- Config Path: `config/experiment/runs/2026-03-14_19-27-26_stage1_local.yaml`
- Experiment Name: `stage1_local`
- Intended Run Dir: `/mnt/data/workspace/runs_root/runs/2026-03-14_19-27-26_stage1_local`
- Final Run Dir: `/mnt/data/workspace/runs_root/runs/2026-03-14_19-27-26_stage1_local`
- LRZ Job ID:
- MCML Job ID:

## Purpose

- Overfit the latent action model locally on Libero-only LeRobot-v3 data instead of the default Octo24 mix.
- Produce a Libero-specialized Stage 1 checkpoint for downstream Stage 3 latent supervision experiments on Libero.
- This run is the checkpoint source later consumed by `2026-03-15_01-43-48_stage3_local_libero_95latent_5mt_balanced_bs64_latent0p1`.

## Config Delta Vs Default

- Override `data` from the default `octo24` mix to `libero`.
- Keep the local rich Stage 1 validation preset from `stage1_local`.
- Pin `logging.runs_dir` to `/mnt/data/workspace/runs_root/runs/2026-03-14_19-27-26_stage1_local` so the documented config reproduces the historical run directory.
- Update `experiment.description` for the actual Libero-only objective of this run.

## Upstream Artifacts / Checkpoints

- Type: `stage1 checkpoint init`
  Source Run: `none`
  Path: `null`
  Notes: The Stage 1 run starts from scratch; `training.load_weights_from` and `training.resume_from_checkpoint` were `null`.

## Launch Command

```bash
conda run -n hlrp python scripts/2_train_stage1_lam.py experiment=stage1_local data=libero
```

## Results / Findings

- Launched locally on `tueilsy-st-022` at `2026-03-14 19:27:30 CET` according to W&B metadata, using the `hlrp` conda env on the RTX 5090 workstation.
- The run used `HuggingFaceVLA/libero` as a 100% single-source training dataset and kept the Stage 1 rich validation setup.
- The run produced checkpoints at `40k`, `45k`, and `50k` steps plus `checkpoints/last.ckpt`.
- `checkpoints/last.ckpt` was written at `2026-03-15 01:11:04 CET` and stores `global_step=53000`, `epoch=5`.
- W&B summary was last updated at `2026-03-15 01:13:55 CET`, which is consistent with the run ending shortly after the 53k checkpoint rather than reaching the configured `200000` max steps.
- Final logged metrics from W&B: `train/loss=8.78e-4`, `val/loss=1.09e-3`, `perf/steps_per_sec=2.69`, `train/unique_codes_in_batch=61`, `val/codebook_entry_utilization_val_cache_all=0.06445`.
- W&B run id: `x665szpk`.

## Downstream Usage

- This run's `checkpoints/last.ckpt` is the explicit `artifacts.lam_checkpoint_path` used by `2026-03-15_01-43-48_stage3_local_libero_95latent_5mt_balanced_bs64_latent0p1`.
