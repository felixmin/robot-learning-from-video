# @package _global_

defaults:
  - _stage3_base
  - /cluster@cluster: mcml_x100
  - /runtime: stage3_cluster
  - optional /user_config: local

experiment:
  name: stage3_cluster
  description: "Cluster Stage 3 Libero training (HLRP shared SmolVLA policy)"

lerobot:
  num_workers: 12
  distributed_timeout_s: 10000

eval:
  batch_size: 5
